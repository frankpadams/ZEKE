const fs=require('fs');
const app=fs.readFileSync('assets/app.js','utf8');
const css=fs.readFileSync('assets/styles.css','utf8');
const must=(x,msg)=>{if(!x)throw new Error(msg)};
const dashboard=app.slice(app.indexOf('function dashboardHTML()'),app.indexOf('function isSuppressedIntegrityArtifact'));
must(dashboard.includes('dashboard-v46'),'v0.46 dashboard wrapper missing');
must(dashboard.includes('dashboardStoryCardsHTML'),'priority insight missing');
must(dashboard.includes('truthfulRecentActivityHTML'),'recent activity missing');
must(dashboard.includes('todayActionsHTML'),'today actions missing');
must(dashboard.includes('upcomingHTML'),'upcoming context missing');
must(dashboard.includes('reviewStatusHTML'),'integrity review status missing');
must(dashboard.includes('thinkingHTML'),'cross-domain insights missing');
must(dashboard.includes('healthGlanceHTML(4)'),'dashboard health snapshot must remain selective');
must(dashboard.includes('timelineSnapshotHTML'),'cross-domain timeline missing');
must(dashboard.includes('weeklyPlanHTML')&&dashboard.includes('coachHTML'),'fitness planning/coaching missing');
must(css.includes('.dashboard-v46-top')&&css.includes('.dashboard-v46-middle'),'desktop composition rules missing');
must(css.includes('.recent-activity-scroll'),'intentional recent-activity scan window missing');
must(css.includes('@media(max-width:820px)'),'responsive dashboard layout missing');
console.log('dashboard v0.46 UX acceptance checks passed');
